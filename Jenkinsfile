pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/BitaouiOussama/GestionProduct.git'
            }
        }
        
}
}
